<Page
    x:Class="Frontend.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Frontend"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" Text="Login BookHub"
                   VerticalAlignment="Bottom" Grid.Row="0"
                   Style="{StaticResource SubheaderTextBlockStyle}"
                   Foreground="{ThemeResource SystemAccentColorDark2}"/>
        <TextBox x:ConnectionId='2' x:Name="nameBox" HorizontalAlignment="Center" Grid.Row="2" Width="350" FontSize="18"
                 Header="User name" PlaceholderText="Your user name or e-mail" VerticalAlignment="Center"/>
        <PasswordBox x:ConnectionId='3' x:Name="passBox" HorizontalAlignment="Center" Grid.Row="3" VerticalAlignment="Center"
                     Width="350" Header="Password" PlaceholderText="Your password" FontSize="18"/>
        <Button x:ConnectionId='4' x:Name="btn" Content="Confirm" Grid.Column="1" Grid.Row="5" VerticalAlignment="Top"
                HorizontalAlignment="Center" FontSize="22" MinWidth="120"
                Style="{StaticResource AccentButtonStyle}"
                                    />
        <ProgressRing x:ConnectionId='5' x:Name="progress" Grid.Row="5" Width="50" Height="50" IsActive="False"/>
    </Grid>
</Page>

